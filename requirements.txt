# Runtime
python-pptx>=1.0
openpyxl>=3.1
Pillow>=10
fonttools>=4.40
numpy>=1.26

# Tests
pytest>=8

# Optional: real-PowerPoint verification (verify_render.py), Windows only
pywin32>=306; sys_platform == "win32"
